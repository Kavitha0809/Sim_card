import React, { useState } from "react";
import styles from "../styles/Initiate_page_Styles/Rectangle3463610.module.css";

const SimCardForm3 = ({ onSubmit }) => {
  // Removed employeeName state
  return (
    <div className={styles.rectangle} style={{minHeight: "100px"}}>
      <div className={styles.inner} style ={{paddingBottom:'40px'}}>
      </div>
    </div>
  );
};

export default SimCardForm3;